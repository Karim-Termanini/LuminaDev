name: Quality Gate

on:
  push:
    branches: [main, 'phase-*', 'stabilization/*', 'feat/*', 'fix/*', 'chore/*', 'docs/*']
  pull_request:
    branches: [main]

jobs:
  quality-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'pnpm'
      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
      - name: Install Linux build deps for Tauri
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential pkg-config libglib2.0-dev libwebkit2gtk-4.1-dev libsoup-3.0-dev libjavascriptcoregtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev librsvg2-bin patchelf libfuse2
      - name: Normalize GitHub git protocol to HTTPS
        run: |
          git config --global url."https://github.com/".insteadOf git@github.com:
          git config --global url."https://github.com/".insteadOf ssh://git@github.com/
      - run: pnpm install --frozen-lockfile
      - name: Lint + typecheck + unit tests
        run: bash scripts/smoke-ci.sh

  dependency-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'pnpm'
      - name: Normalize GitHub git protocol to HTTPS
        run: |
          git config --global url."https://github.com/".insteadOf git@github.com:
          git config --global url."https://github.com/".insteadOf ssh://git@github.com/
      - run: pnpm install --frozen-lockfile
      - name: Production dependency audit
        run: pnpm audit --prod --audit-level=high

  unit-roundtrip-contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'pnpm'
      - name: Normalize GitHub git protocol to HTTPS
        run: |
          git config --global url."https://github.com/".insteadOf git@github.com:
          git config --global url."https://github.com/".insteadOf ssh://git@github.com/
      - run: pnpm install --frozen-lockfile
      - name: Contract error roundtrip tests
        run: pnpm test:roundtrip
      - name: Critical scenario + module availability tests
        run: pnpm test:e2e
      - name: Coverage (critical IPC files)
        run: pnpm test:coverage

  native-linux-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'pnpm'
      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: apps/desktop/src-tauri -> target
      - name: Install Linux build deps for Tauri
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential pkg-config libglib2.0-dev libwebkit2gtk-4.1-dev libsoup-3.0-dev libjavascriptcoregtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev librsvg2-bin patchelf libfuse2
      - name: Normalize GitHub git protocol to HTTPS
        run: |
          git config --global url."https://github.com/".insteadOf git@github.com:
          git config --global url."https://github.com/".insteadOf ssh://git@github.com/
      - run: pnpm install --frozen-lockfile
      - name: Rust smoke tests (Docker + Job Runner + domain integration)
        run: |
          cd apps/desktop/src-tauri
          cargo test --test docker_smoke -- --nocapture
          cd ../../..
          bash scripts/rust-domain-smoke.sh
          cd apps/desktop/src-tauri
          cargo test job_runner -- --nocapture
      - name: Build frontend bundle
        run: pnpm --filter desktop build:web
      - name: Build Tauri Linux app
        run: pnpm --filter desktop build:tauri
