# Desktop GUI dev stack
# All ports use env vars with non-overlapping defaults to prevent collisions across profiles.

services:
  xpra:
    image: jare/xpra:latest
    ports:
      - '${XPRA_PORT:-30600}:14500'
    environment:
      - DISPLAY=:0
    restart: unless-stopped
