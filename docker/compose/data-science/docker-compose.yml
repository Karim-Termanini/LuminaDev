# Minimal preset stack (reliable everywhere). Replace with Jupyter/DataScience stack when ready.
name: lumina-data-science

services:
  stub:
    image: alpine:latest
    command: ["sleep", "infinity"]
    restart: unless-stopped
