# Minimal preset stack (reliable everywhere). GPU/Jupyter stacks belong in a follow-up overlay.
name: lumina-ai-ml

services:
  stub:
    image: alpine:latest
    command: ["sleep", "infinity"]
    restart: unless-stopped
