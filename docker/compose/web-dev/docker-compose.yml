# Minimal preset stack (reliable everywhere). Replace with a real web stack when ready.
name: lumina-web-dev

services:
  stub:
    image: alpine:latest
    command: ["sleep", "infinity"]
    restart: unless-stopped
