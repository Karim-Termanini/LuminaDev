# Minimal preset stack (reliable everywhere). Replace with Hugo/Docusaurus when ready.
name: lumina-docs

services:
  stub:
    image: alpine:latest
    command: ["sleep", "infinity"]
    restart: unless-stopped
