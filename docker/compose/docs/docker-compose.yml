# LUMINA_DEV_COMPOSE_FULL=1 overlay — docs/writing stack

services:
  mkdocs:
    image: squidfunk/mkdocs-material:latest
    ports:
      - "18000:8000"
    volumes:
      - ${PROJECT_DIR}:/docs
    restart: unless-stopped
