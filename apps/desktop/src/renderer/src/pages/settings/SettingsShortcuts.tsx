import type { ReactElement } from 'react'
export function SettingsShortcuts(): ReactElement {
  return <div>Shortcuts — coming soon</div>
}
export function buildChord(_e: { ctrlKey: boolean; shiftKey: boolean; altKey: boolean; metaKey: boolean; key: string }): string | null {
  return null
}
