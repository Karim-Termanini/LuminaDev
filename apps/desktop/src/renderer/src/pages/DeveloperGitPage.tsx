import type { ReactElement } from 'react'

import { GitAssistantPage } from './git/GitAssistantPage'

/** Git Assistant — single-page save & share UX (legacy tabbed UI removed). */
export function DeveloperGitPage(): ReactElement {
  return <GitAssistantPage />
}
